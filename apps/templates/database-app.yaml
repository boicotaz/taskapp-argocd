apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: taskapp-database
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: {{ .Values.notifications.slack.channel | quote }}
    notifications.argoproj.io/subscribe.on-sync-failed.slack: {{ .Values.notifications.slack.channel | quote }}
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL | quote }}
    targetRevision: {{ .Values.targetRevision | quote }}
    path: database
    helm:
      parameters:
        - name: externalSecrets.secretPath
          value: {{ .Values.database.secretPath | quote }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: default
  {{- include "taskapp.ignoreDifferences" (dict "kind" "StatefulSet") | nindent 2 }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
