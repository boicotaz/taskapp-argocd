apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: taskapp-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/boicotaz/taskapp-argocd.git"
    targetRevision: main
    path: apps
    helm:
      valueFiles:
        - values-dev.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
